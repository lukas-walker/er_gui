the web gui for the escape room



## Installation of testing environment on Windows

```
# make new venv
python -m venv --system-site-packages venv

# windows
venv\Scripts\activate

# pi
source venv/bin/activate

pip install -r requirements.txt
```